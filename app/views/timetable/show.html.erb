<h1>Timetable#show</h1>

<div>
	<% @ttlist.each do |tt| %>
		<%= form_tag "/timetable/destroy/#{tt.id}" do %>
			<%= tt.lectures %>
			<%= submit_tag '삭제', id: 'btn-delete' %>
		<% end %>
	<% end %>
</div>
